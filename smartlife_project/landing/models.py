from django.db import models

# All content models have been moved to dashboardmanager.models
# This file now only contains imports for backward compatibility
from dashboardmanager.models import (
    HeroSection, Feature, Testimonial, FAQ, ContactMessage, Newsletter
)
